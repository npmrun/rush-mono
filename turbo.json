{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "watch": {},
    "dev": {},
    "develop": {
      "dependsOn": ["^watch", "dev"],
      "cache": false
    }
  }
}
