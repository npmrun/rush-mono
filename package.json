{
    "private": true,
    "scripts": {
        "preinstall": "npx only-allow pnpm",
        "new": "writefile",
        "build": "turbo run build",
        "build:cli": "turbo run build --filter @internal/build",
        "dev:mask": "turbo run dev --filter @rush-ui/mask",
        "dev:test": "turbo run dev --filter @rush-ui/test",
        "dev": "turbo run dev --parallel",
        "release": "changeset"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@changesets/cli": "^2.24.3",
        "@internal/tsconfig": "workspace:*",
        "tsx": "^3.9.0",
        "turbo": "^1.4.5"
    }
}
