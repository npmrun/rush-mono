{
    "private": true,
    "scripts": {
        "preinstall": "npx only-allow pnpm",
        "dev": "turbo run dev --parallel",
        "build": "turbo run build",
        "dev:cli": "turbo run dev --filter @internal/build",
        "build:cli": "turbo run build --filter @internal/build",
        "dev:ccc": "turbo run dev --filter @rush-mono/ccc",
        "dev:mask": "turbo run dev --filter @rush-mono/mask",
        "dev:test": "turbo run dev --filter @rush-mono/test",
        "release": "changeset"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@changesets/cli": "^2.24.3",
        "@internal/tsconfig": "workspace:*",
        "tsx": "^3.9.0",
        "turbo": "^1.4.5"
    }
}
